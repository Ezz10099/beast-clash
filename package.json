{
  "name": "pixel-mage",
  "version": "0.1.0-rc.1",
  "private": true,
  "description": "A tiny mobile-first five-wave pixel-action game.",
  "scripts": {
    "build": "node scripts/build-web.mjs",
    "check": "node --check game.js && node scripts/check.mjs && npm run build && node scripts/check-build.mjs",
    "preview": "npm run build && python3 -m http.server 4173 --directory dist"
  }
}
